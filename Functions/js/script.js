/**
 * Created by Ceugley on 1/26/15.
 */


// Returning Values

var total = calcArea (30, 20);

function calcArea(w, h){
    var area = w * h;
    return area;  //function is spitting the info out, but must be stored somewhere
}
console.log(total);

